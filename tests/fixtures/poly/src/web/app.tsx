import {
  connect,
  Pool,
} from "@lib/db";
// import { evil } from "@lib/should-not-be-seen";
const s = "this // is not a comment and require('nope') is not an import";
import type { Thing } from "./types";
export const App = 1;
